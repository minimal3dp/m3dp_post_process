version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
    # Network configuration allows app to connect to db on localhost
    # Uncomment depends_on when enabling PostgreSQL
    # depends_on:
    #   - db

  # PostgreSQL database service (currently commented out)
  # Uncomment when ready to add database functionality
  # db:
  #   image: postgres:16-alpine
  #   restart: unless-stopped
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   environment:
  #     POSTGRES_USER: m3dp
  #     POSTGRES_PASSWORD: m3dp_dev_password
  #     POSTGRES_DB: m3dp_post_process
  #   ports:
  #     - "5432:5432"

volumes:
  postgres-data:

# Notes for using with Podman:
# 1. Podman socket should be aliased to Docker socket in devcontainer.json mounts
# 2. Run with: podman-compose up -d
# 3. To enable PostgreSQL, uncomment the depends_on and profiles sections above
